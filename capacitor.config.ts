import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.angular.with.icons',
  appName: 'cs-angular-with-icons',
  webDir: 'www/browser',
  server: {
    androidScheme: 'https'
  }
};

export default config;
